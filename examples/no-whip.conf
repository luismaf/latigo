# Panes or agent names that must never be whipped. One per line, # comments.
#
# This is a MANUAL OVERRIDE, not the real defence. The real defence is the
# worker-kind filter: only panes running the worker kind are workers, so a
# conversation pane is skipped even when this file goes stale.
#
# Note that pane ids change when a session is reopened, which is exactly how
# id-based lists rot. If a pane you listed here starts receiving items, this
# is the line that went stale.
w1:p9        # someone's interactive session
supervisor   # names work too
